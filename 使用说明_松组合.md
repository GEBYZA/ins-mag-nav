# 松组合 INS + 全球地磁模型（WMM）融合

## 你的需求对应关系

| 你的要求 | 本系统实现 |
|----------|------------|
| **全球地磁图先验** | NOAA **WMM2020** 系数（`data/WMM2020.COF`），全球任意经纬高可查磁场；可 `python -m ins_mag_nav.download_wmm` 从网络更新 |
| **输入九轴 IMU** | 每帧 `gx,gy,gz, ax,ay,az, mx,my,mz` |
| **输出末位置** | 纬度、经度、海拔 |
| **输出末姿态/速度/加速度** | roll/pitch/yaw，vn/ve/vd，an/ae/ad |
| **松组合卡尔曼** | 惯导主递推 + 地磁/加计外源观测更新 |
| **抗噪与鲁棒** | 磁力中值滤波、NIS 门限、Huber 降权、场强一致性、低动态才用加计 |

> 说明：WMM 是**全球主磁场模型**（与 NOAA 官网一致），不是厘米级磁异常图。长距离**位置**约束较弱，主要改善**姿态/航向**；要米级地磁定位需额外磁异常图（如 EMAG2）。

## 数据文件

- `data/WMM2020.COF` — 已内置 NOAA 2020 高斯系数（全球）
- `data/global_wmm_grid_5deg.npz` — 可选 5° 全球网格缓存（运行 download_wmm 生成）

## Python 用法

```python
from ins_mag_nav import FusionSession, InitialState, ImuMagSample

session = FusionSession(dt_imu=0.01)  # 自动加载全球 WMM
session.set_initial(InitialState(
    latitude_deg=39.9042, longitude_deg=116.4074, altitude_m=50.0,
    roll_deg=0, pitch_deg=0, yaw_deg=0,
))
r = session.run_samples([
    ImuMagSample(0,0,0, 0,0,-9.81, 20,0,35),
    ImuMagSample(0,0,0.052, 0,0,-9.81, 19.8,0.5,35),
])
print(r.as_dict())
```

## 命令行

```powershell
cd c:\Users\lenovo\Documents
python -m ins_mag_nav.download_wmm
python -m ins_mag_nav.run_user_example ins_mag_nav\data\mission_example.txt
```

## 网页

双击 `input_demo.html`（浏览器内为 WMM 简化实现；**完整 WMM 请用 Python**）。

## 单位

- 陀螺 rad/s，加计 m/s²，磁力计 nT（μT 亦可，与地图单位一致即可）
- 航向 0° = 北，NED 坐标系
